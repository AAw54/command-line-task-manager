# command-line-task-manager
A command-line task manager built in Python. It allows users to add, view, complete, and delete tasks, with data persistently stored in a JSON file and automatically saved even on unexpected exits. It also includes input validation to handle invalid user input
